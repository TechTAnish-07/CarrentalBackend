spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/car_rental_db
    username: tanish
    password: your_new_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: patidartanish31@gmail.com
    password: jwjs mkoe sxpv sjdi   # 👈 IMPORTANT
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

cloudinary:
            cloud_name: ${CLOUDINARY_CLOUD_NAME:dabepvflo}
            api_key: ${CLOUDINARY_API_KEY:186665796711431}
            api_secret: ${CLOUDINARY_API_SECRET:cWFVWTqnMbyhjzkY30Q1WeembQ4}
jwt:
  secret: ${JWT_SECRET:THIS_IS_A_LONG_SECRET_KEY_1234567890_ABCD}
  expiration-ms: 86400000   # 24 hours

logging:
  level:
    com.sangraj.carrental.config.JwtAuthFilter: DEBUG
    org.springframework.security: DEBUG
